services:
  - type: web
    name: stock-analyzer-portal
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt && npm install --prefix frontend && npm run build --prefix frontend"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
      - key: DATABASE_URL
        value: postgresql://postgres:[YOUR-PASSWORD]@db.vjjoheppyhqrlqplheog.supabase.co:5432/postgres
      # Add this so python stdout logs print in real-time
      - key: PYTHONUNBUFFERED
        value: "true"
